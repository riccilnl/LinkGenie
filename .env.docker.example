# Docker 部署环境变量配置示例
# 复制此文件为 .env 并填写实际值

# ========================================
# 必填配置
# ========================================

# API 认证 Token（必须修改为强密码！）
# 生成方法: openssl rand -base64 32
API_TOKEN=your-strong-random-token-here

# Google Gemini API 密钥（必填，用于 AI 增强功能）
# 获取地址: https://makersuite.google.com/app/apikey
AI_API_KEY=your-gemini-api-key-here

# ========================================
# 可选配置
# ========================================

# AI API 基础地址（通常不需要修改）
AI_API_BASE=https://generativelanguage.googleapis.com/v1beta

# 服务端口（容器内部端口，通常不需要修改）
PORT=8000

# ========================================
# 使用说明
# ========================================
# 1. 复制此文件: cp .env.docker.example .env
# 2. 编辑 .env 文件，填写实际的 Token 和 API Key
# 3. 启动服务: docker-compose -f docker-compose.go.yml up -d
# 4. 在前端设置中配置相同的 API_TOKEN
